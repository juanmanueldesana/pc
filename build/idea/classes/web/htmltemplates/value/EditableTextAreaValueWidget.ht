<textarea style="${heightOrMaxHeight}" disabled="${disabled}" name="${renderId}" maxlength="${maxlength}"
          placeholder="${placeholder}"
          class="${noTrack ? 'gw-noTrack'}" ${systemAttributes}>${currentValue}</textarea>
