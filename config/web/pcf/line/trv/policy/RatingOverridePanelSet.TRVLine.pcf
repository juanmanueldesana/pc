<?xml version="1.0"?>
<PCF
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:noNamespaceSchemaLocation="../../../../../../../pcf.xsd">
  <PanelSet
    id="RatingOverridePanelSet"
    mode="TRVLine">
    <Require
      name="period"
      type="PolicyPeriod"/>
    <Variable
      initialValue="period.TRVLine"
      name="line"
      type="TRVLine"/>
    <Variable
      initialValue="new gw.lob.trv.financials.TRVQuoteDisplayUtil(line, true)"
      name="quoteDisplayUtil"
      type="gw.lob.trv.financials.TRVQuoteDisplayUtil"/>
    <Variable
      initialValue="quoteDisplayUtil.getCoverablesWithCostsByType()"
      name="cblByType"
      type="Map&lt;gw.lang.reflect.IType, List&lt;Coverable&gt;&gt;"/>
    <Variable
      initialValue="quoteDisplayUtil.getCostsByCoverable()"
      name="costsByCbl"
      type="Map&lt;Coverable, List&lt;TRVCost&gt;&gt;"/>
    <Variable
      initialValue="costsByCbl.get(line)"
      name="lineLevelCosts"
      type="List&lt;TRVCost&gt;"/>
    <Variable
      initialValue="CurrentLocation.InEditMode"
      name="isEditable"
      type="boolean"/>
    <Variable
      initialValue="period.EditEffectiveDate"
      name="jobEffDate"
      type="Date"/>
    <Variable
      initialValue="period.PreferredSettlementCurrency"
      name="prefCurrency"
      type="Currency"/>
    <PanelIterator
      elementName="cblType"
      id="cblTypeIterator"
      value="cblByType.Keys.where( \ t -&gt; t != productmodel.TRVLine)"
      valueType="List&lt;gw.lang.reflect.IType&gt;">
      <IteratorSort
        sortBy="cblType.toString()"
        sortOrder="1"/>
      <PanelIterator
        elementName="cbl"
        id="cblIterator"
        value="cblByType.get(cblType).toTypedArray()"
        valueType="Coverable[]">
        <IteratorSort
          sortBy="cbl.FixedId"
          sortOrder="1"/>
        <DetailViewPanel>
          <InputColumn>
            <Label
              label="quoteDisplayUtil.CoverableDisplayName(cbl)"/>
          </InputColumn>
        </DetailViewPanel>
        <DetailViewPanel>
          <InputColumn>
            <ListViewInput
              def="TRVRatingOverrideCostLV(costsByCbl.get(cbl), quoteDisplayUtil, isEditable, jobEffDate, prefCurrency)">
	      <Toolbar/>
	    </ListViewInput>
          </InputColumn>
        </DetailViewPanel>
      </PanelIterator>
    </PanelIterator>
    <DetailViewPanel
      visible="lineLevelCosts.Count&gt;0">
      <InputColumn>
        <Label
          label="DisplayKey.get(&quot;Web.Policy.TRV.Financials.LineLevelPremiums&quot;)"/>
      </InputColumn>
    </DetailViewPanel>
    <DetailViewPanel
      visible="lineLevelCosts.Count&gt;0">
      <InputColumn>
        <ListViewInput
          def="TRVRatingOverrideCostLV(lineLevelCosts, quoteDisplayUtil, isEditable, jobEffDate, prefCurrency)">
	  <Toolbar/>
        </ListViewInput>
      </InputColumn>
    </DetailViewPanel>
  </PanelSet>
</PCF>